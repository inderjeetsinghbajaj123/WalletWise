/**
 * @interface IMailerService
 * 
 * Contract for email sending operations.
 * 
 * @typedef {Object} IMailerService
 * 
 * @property {function(string, string, string): Promise<void>} sendEmail
 *   Send an email. Parameters: (to, subject, htmlBody).
 */

module.exports = {};
